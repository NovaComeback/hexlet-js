Проект в рамках обучения Хекслет
`make install` — устанавливает все необходимые зависимости проекта (пакеты npm).
```markdown
   make lint — запуск проверки кода линтером
   make lint-fix — автоматическое исправление мелких ошибок линтером
